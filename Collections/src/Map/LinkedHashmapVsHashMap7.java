package Map;

public class LinkedHashmapVsHashMap7 {

	// LinkedHashMap: insertion order is preserved, and stores the value in
	// ******[insertion order means after storing values while printing we get the
	// ******values in same order]
	// node(doubly linked list).
	// k,v format
	// not thread safe (not synchronised)
	// 1 null key and multiple null values are allowed.

	// HashMap: insertion order is not preserved. rest are same as linkedhashmap
	public static void main(String[] args) {

	}

}
